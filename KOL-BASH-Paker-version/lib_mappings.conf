# Synology-specific libraries
synowebapi/webapi.h=-lsynowebapi
synoshare/share.h=-lsynoshare
synomount/mount.h=-lsynomount

# Common NAS operations
mntent.h=-lmount
smbclient.h=-lsmbclient
nfs/nfs.h=-lnfs
quota.h=-lquota

# Backup and storage
tar.h=-ltar
archive.h=-larchive
rsync/rsync.h=-lrsync

# Additional compression (commonly available)
snappy-c.h=-lsnappy

# Network services (commonly used in NAS)
ldap.h=-lldap
krb5.h=-lkrb5
libssh/libssh.h=-lssh
libssh2.h=-lssh2

# File indexing and search
magic.h=-lmagic
libexif/exif-data.h=-lexif

